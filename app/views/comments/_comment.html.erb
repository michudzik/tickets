<tr data-comment-id="<%= comment.id %>">
	<td>
		<p>
			<strong>Body:</strong>
			<%= comment.body %>
			<br />
			<%= link_to 'Destroy', [comment.user, comment], method: :delete, remote: true, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
		</p>
	</td>
</tr>