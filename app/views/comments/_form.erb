<!-- <<<<<<< HEAD
<%= form_for [current_user, current_user.comments.build], remote: true, html: {class: 'new-comment'} do |f| %>
======= -->
<% @tichet = Ticket.find(params[:id]) %>

<%= form_for @comment, url: comments_path do |f| %>
<!-- >>>>>>> staging -->
	<%= f.label :body %>
	<%= f.text_field :body %>
	<hr />
	<%= f.hidden_field(:user_id, value: current_user.id) %>
	<hr />
	<%= f.hidden_field(:ticket_id, value: @ticket.id) %>
	<hr />
	<%= f.submit 'Save' %>	
<% end %>